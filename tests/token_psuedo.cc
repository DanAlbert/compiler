int main(){
	int number;
	number = (rand() %100);
	
	//I still need to hook this up to the input of compiler somehow. 
	//I'll finish this by tomorrow
			
	if (number < 10)
		array.add("1");	
	else if (number <20)
		array.add("string");
	else if (number <30)
		array.add("+");
	else if (number <40)
		array.add("(");
	else if (number <50)
		array.add(")");
	else if (number <60)
		array.add("*");
	else if (number < 70)
		array.add("2");	
	else if (number < 80)
		array.add("1000");	
	else if
		array.add("))");



}
