






(defun project (y x)
  (let (
        (ip-xy (sum (* x y)))
        (ip-xx (sum (* x x)))
        )
    (* (/ ip-xy ip-xx) x))
)

(defun project (y x)
  (let (
        (ip-xy (sum (* x y)))
        (ip-xx (sum (* x x)))
        )
    (* (/ ip-xy ip-xx) x))
)


(defun project (y x)
  (let (
        (ip-xy (sum (* x y)))
        (ip-xx (sum (* x x)))
        )
    (* (/ ip-xy ip-xx) x))
)


(defun project (y x)
  (let (
        (ip-xy (sum (* x y)))
        (ip-xx (sum (* x x)))
        )
    (* (/ ip-xy ip-xx) x))
)


(defun project (y x)
  (let (
        (ip-xy (sum (* x y)))
        (ip-xx (sum (* x x)))
        )
    (* (/ ip-xy ip-xx) x))
)


(defun project (y x)
  (let (
        (ip-xy (sum (* x y)))
        (ip-xx (sum (* x x)))
        )
    (* (/ ip-xy ip-xx) x))
)


